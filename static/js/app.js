const $=(s,c=document)=>c.querySelector(s),$$=(s,c=document)=>[...c.querySelectorAll(s)];let observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});$$('.reveal').forEach(e=>observer.observe(e));const modal=$('#androidModal');$$('.android-trigger').forEach(b=>b.addEventListener('click',()=>modal?.classList.add('open')));modal?.querySelector('.modal-close')?.addEventListener('click',()=>modal.classList.remove('open'));modal?.addEventListener('click',e=>{if(e.target===modal)modal.classList.remove('open')});$('#copyUrl')?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText($('#mobileUrl').textContent);toast('URL disalin')}catch{toast('Salin URL secara manual')}});const staticSite=document.body.dataset.staticSite==='1';if(staticSite){const s=$('#publicStatus');if(s)s.innerHTML='✓ Akses publik aktif melalui GitHub Pages. QR siap digunakan dari perangkat lain.';const configured=document.body.dataset.siteUrl||location.origin;const mobile=configured.replace(/\/$/,'')+'/?device=android';const code=$('#mobileUrl');if(code)code.textContent=mobile;$$('.android-trigger').forEach(b=>b.dataset.url=mobile)}else{fetch('/api/public-config').then(r=>r.json()).then(d=>{const s=$('#publicStatus');if(s)s.innerHTML=d.isPublicConfigured?'✓ Akses publik aktif. QR siap digunakan dari perangkat lain.':'Saat ini aplikasi berjalan secara lokal. Aktifkan alamat publik agar QR dapat digunakan dari koneksi internet yang berbeda.'}).catch(()=>{})};$$('.lightbox-trigger').forEach(b=>b.addEventListener('click',()=>{const l=$('#lightbox');$('img',l).src=b.dataset.src;$('p',l).textContent=b.dataset.title;l.classList.add('open')}));$('#lightbox')?.querySelector('button')?.addEventListener('click',()=>$('#lightbox').classList.remove('open'));function toast(m){const t=$('#toast');if(!t)return;t.textContent=m;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2200)}const canvas=$('#mesh');if(canvas){const c=canvas.getContext('2d');let pts,w,h,dpr;function rs(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=w*dpr;canvas.height=h*dpr;c.setTransform(dpr,0,0,dpr,0,0);pts=Array.from({length:Math.min(60,Math.floor(w/20))},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.2,vy:(Math.random()-.5)*.2}))}function dr(){c.clearRect(0,0,w,h);pts.forEach(p=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>w)p.vx*=-1;if(p.y<0||p.y>h)p.vy*=-1;c.beginPath();c.arc(p.x,p.y,1,0,Math.PI*2);c.fillStyle='rgba(151,0,32,.16)';c.fill()});requestAnimationFrame(dr)}addEventListener('resize',rs);rs();dr()}